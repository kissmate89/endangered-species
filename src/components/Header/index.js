import React from "react";

import styles from "./header.scss";

const Header = () => (
  <div className={styles.headerWrapper}>
    <h1 className={styles.headerTitle}>Endangered species</h1>
  </div>
);

export default Header;
